#include <stdio.h>


typedef struct {
    int id; 
    float area = 1521.11; //Em KM²;
    float pib = 699.280; //Bilhões de Reais;
    float area2 = 1200.25; //Em KM²;
    float pib2 = 300.500; //Bilhões de Reais;
    

// Função para realizar a soma usando switch
    void  (area + pib), (area2 + pib2) {
    switch (tipo_soma) {
        case 1: // Soma de áreas e pibs carta1 
            printf("A soma da área + pib carta1 é: %.2f\n");
            scanf("%f", &dados.area + dados.pib);
            break;
        case 2: // Soma de areas e pibs carta2
            printf("A soma da área2 + pib2 carta2 é: %.2f\n");
            scanf("%f", &dados.area2 + dados.pib2);
            break;
        case 3: // Comparação das somas
            printf("Resultados: %.2f\n");
            scanf("%f", &dados.area + dados.pib > dados.area2 + dados.pib2);
            printf("Carta1 venceu: %.2f\n");
            scanf("%f", &dados.area + dados.pib < dados.area2 + dados.pib2);
            printf("Carta2 venceu: %.2f\n");
            scanf("%f", &dados.area + dados.pib == dados.area2 + dados.pib2);
            printf("Empate: %.2f\n");
            scanf("%f", &dados.area + dados.pib != dados.area2 + dados.pib2);
            printf("Diferente: %.2f\n");
            if (dados.area + dados.pib > dados.area2 + dados.pib2);

    }
    
}   
    return 0;

  // Professora não consegui fazer rodar, mas tentei fazer o máximo que consegui. Passei três dias tentando fazer rodar, mas não consegui.
  //Espero um retorno. Desde já agradeço a atenção.

   